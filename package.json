{
  "scripts": {
    "postinstall": "(cd backend && npm install) && (cd ../frontend && npm install)",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "react-youtube": "^10.1.0"
  }
}
